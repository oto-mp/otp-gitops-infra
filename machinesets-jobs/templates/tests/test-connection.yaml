apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "machinesets-jobs.fullname" . }}-test-connection"
  labels:
    {{- include "machinesets-jobs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "machinesets-jobs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
